# IP Agent 全页评审（R3 后代码）

日期：2026-09-11。范围：只读评测 `/workspace/ip-harness` Agent 壳全部页面；**本文件为唯一写入产物**，未改产品代码。

对照：`AGENT_APPLE_AUDIT_R3.md` 已落地项（新建会话去工程师腔、toast 仅失败抑制、中文回退、Harness 字距、focus-ring 等）已在当前源码核验。

评分轴：**页面逻辑 / 布局 / UI / UX**，各 0–10。

---

## 1. AgentHome · `/agent`

| 维度 | 分 | 一句话 |
|------|----|--------|
| 页面逻辑 | **8.5** | 目标→选人/案件→建会话清晰；URL `?case=`、续会话/续案件、推荐办理人、最近会话闭环完整。 |
| 布局 | **8.5** | 居中 `max-w-2xl` + 主输入卡为视觉锚点；推荐 2 列与最近列表层次清楚。 |
| UI | **8.5** | `soft-card` + `focus-within` 与 Composer 同构；续办芯片色区分好（紫会话 / 绿案件）。 |
| UX | **8.0** | 主路径极顺；洞察快捷默认折叠克制，但硬编码 `c1`/`c5` 且文案「洞察」偏产品内部。 |

**还需优化**
- 洞察快捷回退案件写死 `c1`/`c5`，应优先当前 `caseId` / 最近案件 / 留空并提示选案。
- 推荐卡一键建会话跳过目标编辑，可考虑先填 `agentId` 聚焦输入框而非立刻 `createSession`（或保留但目标文案更具体）。
- 「全部目录」与侧栏「目录」同义，暂可。

---

## 2. AgentCatalogPage · `/agent/agents`

| 维度 | 分 | 一句话 |
|------|----|--------|
| 页面逻辑 | **8.0** | 按阶段筛 + 启动会话 + `<details>` 展 HITL/能力/RACI；信息够用、无多余状态机。 |
| 布局 | **8.5** | 响应式 2/3 列卡片网格舒适；主 CTA 在折叠详情之上，启动优先于浏览。 |
| UI | **8.0** | 状态色（可用/试用/停用）与确认门琥珀芯片一致；详情内 specialty 与摘要略重复。 |
| UX | **8.0** | 「启动会话」主色明确；缺文案搜索与筛选空态；标题「目录」对新手略抽象。 |

**还需优化**
- 阶段筛选无结果时缺 EmptyState（仅空网格）。
- 可补名称/擅长关键词搜索（与侧栏会话搜对称）。
- 详情「擅长」与卡片一行文案重复，可只留 RACI + 能力。

---

## 3. AgentSkills · `/agent/skills`

| 维度 | 分 | 一句话 |
|------|----|--------|
| 页面逻辑 | **7.0** | 只读能力表 + 已知则映射「写入中台」命令；正确但薄，无筛选/关联启动。 |
| 布局 | **7.5** | `PageHeader` + 双列网格整齐；内容短时页显空。 |
| UI | **7.5** | soft-card / mono id / 办理人 chip 一致；偏「内部参考」材质。 |
| UX | **7.0** | 主 CTA「去选办理人启动」正确；与目录「能力」详情高度重叠，独立页价值偏弱。 |

**还需优化**
- 与 Catalog 详情能力块重复：考虑并入目录「能力参考」抽屉，或给 Skills 加按办理人/是否写库筛选。
- 能力卡可链到「用该能力对应办理人启动」（至少跳目录并高亮）。

---

## 4. AgentHarnessOverview · `/agent/harness`

| 维度 | 分 | 一句话 |
|------|----|--------|
| 页面逻辑 | **7.5** | 教育向：唯一运行时五块 + 插件办理人；底部链目录/能力；不承担主办理流。 |
| 布局 | **8.0** | 核心条 → 插件网格 → 三列出口清楚；琥珀警示条位置对。 |
| UI | **8.0** | 渐变壳 + 插件卡；R3 已去掉 `tracking-wider`；办理人卡不可点略「死」。 |
| UX | **7.5** | 适合一次理解架构；藏在侧栏「更多」里，日常用户很少到达；与目录信息重叠。 |

**还需优化**
- 插件办理人卡可点「启动」或链到目录对应卡，避免只展示。
- 侧栏寻路：可考虑「架构说明」升一级（非必须，P2）。

---

## 5. AgentSessionsList · `/agent/sessions`

| 维度 | 分 | 一句话 |
|------|----|--------|
| 页面逻辑 | **8.5** | 共享 `sessionSearch`、重命名、归档/取消归档、空态双出口；与侧栏状态同源。 |
| 布局 | **8.5** | PageHeader + 工具条 + 表；列齐全（标题/办理人/案件/状态/更新/操作）。 |
| UI | **8.5** | soft-card 表、44px 行高、归档徽章克制；操作图标仅悬停/聚焦显。 |
| UX | **8.0** | 整行 Enter/空格可进；案件链 `stopPropagation` 正确；归档无确认（演示可接受）。 |

**还需优化**
- 归档可加轻确认或 toast「已归档 · 撤销」（防误点）。
- 与侧栏列表功能重叠可接受（表是管理视图）；暂可保持双入口。

---

## 6. AgentSessionWorkspace · `/agent/sessions/:id`（含 session 组件）

| 维度 | 分 | 一句话 |
|------|----|--------|
| 页面逻辑 | **9.0** | 横幅互斥队列、确认门角色排序、换人清空门+内联确认、写回 toast、失败重试、无案提示 — 主办理闭环最强。 |
| 布局 | **8.5** | 顶栏 / Timeline / ConfirmBar / Composer / 右栏上下文；右栏默认关，主列专注。 |
| UI | **8.5** | Timeline soft-card 色调分 kind；确认琥珀带 + 步进实心；Composer 确认态 ghost 降级。 |
| UX | **8.5** | 「开始办理」唯一实心主 CTA；预览进 ⋯；中台同步翠绿 toast 可点开案件/工作台；R3 后 toast 不再被 no_case 误杀。 |

**组件快评**
- `SessionConfirmBar`：步进 `firstActionable` 单一主色 ✅；递交归档升实心 ✅。
- `SessionComposer`：`hitlActive` 禁用输入 +「确认完成后再办理」✅；换人内联确认 ✅。
- `SessionTimeline`：用户面 kind 中文；工具详情折叠 JSON — 演示可接受，正式产品可再白话。
- `SessionContextPanel`：案件/期限/产物/办理记录分层好；空态「办理后出现草稿」已去试运行腔 ✅。

**还需优化**
- 编排文件仍 ~680 行，后续可再抽「顶栏/横幅/toast」子组件（工程债，非体验 P0）。
- Timeline「查看调用详情」内 JSON 对终端用户仍偏工程师；默认可只显示中文摘要。
- 顶栏「改名」仅 group-hover 显，键盘 Tab 已 `focus:opacity-100`，暂可。

---

## 7. AgentShell chrome（全局）

| 维度 | 分 | 一句话 |
|------|----|--------|
| 页面逻辑 | **8.0** | 新建、导航、会话搜、状态筛、归档、行菜单重命名/归档；顶栏办理人只读且仅会话页。 |
| 布局 | **8.0** | 顶栏 + 窄侧栏 + main Outlet；会话列表密度合适；筛选激活后「更多筛选」条占高。 |
| UI | **8.5** | 状态点、活动左边线、soft 菜单、skip-link；材质与子页一致。 |
| UX | **7.5** | 默认「全部 | 待你确认」克制好；但 **⋯ 筛选菜单** 与激活后的 **「更多筛选」条** 双层，认知略绕；「架构/全部会话」藏更多。 |

**还需优化**
- 收敛筛选：⋯ 打开即含进行中/已完成/办理人/归档，避免「更多筛选」按钮与 ⋯ 两套入口并存（或文档化单一路径）。
- 窄屏 aside 常驻，无折叠（产品演示桌面优先，正式需汉堡/抽屉）。
- 导航四格挤（首页/目录/能力/更多），小宽度截断风险 — 暂可。

---

## 分页汇总表

| 页面 | 逻辑 | 布局 | UI | UX | 均分 |
|------|------|------|----|----|------|
| AgentHome `/agent` | 8.5 | 8.5 | 8.5 | 8.0 | **8.4** |
| AgentCatalogPage `/agent/agents` | 8.0 | 8.5 | 8.0 | 8.0 | **8.1** |
| AgentSkills `/agent/skills` | 7.0 | 7.5 | 7.5 | 7.0 | **7.3** |
| AgentHarnessOverview `/agent/harness` | 7.5 | 8.0 | 8.0 | 7.5 | **7.8** |
| AgentSessionsList `/agent/sessions` | 8.5 | 8.5 | 8.5 | 8.0 | **8.4** |
| AgentSessionWorkspace `…/:id` | 9.0 | 8.5 | 8.5 | 8.5 | **8.6** |
| AgentShell chrome | 8.0 | 8.0 | 8.5 | 7.5 | **8.0** |

**平台页均分 ≈ 8.1**（会话工作区最高；Skills / Harness 拖后腿但定位为参考页）。

---

## 总体结论：是否还需大优化？

**诚实结论：可以收口。暂无 P0。**

R3 后主办理路径（Home → 会话 → 确认 → 写回中台）在逻辑/CTA/文案/反馈上已达「可对外演示的抛光壳」：确认态单一主色、换人不静默丢门、横幅队列、toast 与常驻带共存、用户面无 Harness/mock/试运行。  
**不需要再做信息架构重做或功能堆叠。** 剩余为 P2 级抛光与参考页价值提升；Skills/Harness 分数偏低是「参考页偏薄」，不是主流程断裂。

若只修体验债：**优先 Shell 筛选认知 + Home 洞察硬编码案件**；其余可进维护 backlog。

---

## Top remaining backlog（≤6，无则可不做）

1. **Shell 筛选双入口收敛**（⋯ vs「更多筛选」）— UX 7.5 主因，改动局部。
2. **Home 洞察快捷去掉硬编码 `c1`/`c5`** — 用当前/最近案件或强制选案。
3. **Catalog 筛选空态 + 可选关键词搜** — 与会话搜对称。
4. **Skills 减重叠**：并入目录或加「按办理人/是否写库」筛 + 启动链。
5. **Harness 插件卡可启动**（或深链目录）— 参考页变可行动。
6. **Timeline 工具详情默认白话摘要**（JSON 仍可折叠）— 终端用户零行话最后一公里。

（归档误触确认、Workspace 再拆文件 — 可选，不占 Top6 硬位。）

---

## 方法备忘

- 通读：`AgentHome` / `AgentCatalogPage` / `AgentSkills` / `AgentHarnessOverview` / `AgentSessionsList` / `AgentSessionWorkspace` + `session/*` / `AgentShell`；核验 `App.tsx` 路由与 R3 文案/toast 条件。
- 未跑浏览器截图；结论基于当前 R3 后源码结构与交互代码路径。

---

## P2 + 去 AI 模板味（已落地 · 2026-09-11）

评价轴仍用 **web apple-design 克制**（Purpose / Simplicity / Craft），**不是**完整原生 iOS HIG。

### A · Top6 P2

1. **Shell 筛选单入口**：去掉「更多筛选」与 ⋯ 双路径；侧栏「筛选」菜单唯一承载进行中/已完成/办理人/归档；列表页文案标明状态筛选在左侧，共用 `sessionSearch`。
2. **Home 洞察快捷**：用当前 `caseId` → `lastVisited` 案件；皆空则提示「请先选择关联案件」，不再硬编码 `c1`/`c5`。
3. **Catalog**：阶段/关键词空结果用 `EmptyState`；新增名称/擅长搜索；支持 `?agent=` / hash 高亮。
4. **Skills**：按办理人 +「可写中台」筛选；卡可「用此办理人启动」或深链目录。
5. **Harness 插件卡**：可点启动会话，或链 `/agent/agents?agent=`。
6. **Timeline**：默认「查看办理细节」白话摘要（动作/参数/结果）；原始 JSON 折叠为可选项。

### B · 去 AI 模板味（强化）

- 杀 card-in-card：侧栏/首页推荐/时间线/能力改 **扁平行 + 分割线**，少 soft-card 阴影叠层。
- 主 CTA **slate-900**；indigo 仅留给产品切换等极少数强调（壳标「IP 办理」用近黑）。
- 侧栏 Mail 感：白底、`border-r`、无糖果 pill；会话行靠状态点 + inset 左边线。
- Home 左对齐工作台头「说明要办的事」+ 发丝边输入，非居中 chatbot hero。
- 确认条：`border-l-4` 琥珀强调，非整条黄糖果；去 Sparkles 装饰。

