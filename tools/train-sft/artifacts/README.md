# artifacts/

Large checkpoint binaries are **gitignored**.  
Committed content: `.gitkeep`, this note, and tiny stub JSON/metrics produced by the default runner path.

See `checkpoints/<run_id>/` after `python tools/train-sft/scripts/run_train_sft.py`.
