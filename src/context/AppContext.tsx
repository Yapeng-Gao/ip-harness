/**
 * Compat re-export — AppProvider / useApp live in @ip/app-state (Phase 2).
 * Values only; no local use. Callers keep `@shared/context/AppContext`.
 */
export { AppProvider, useApp } from '@ip/app-state'
