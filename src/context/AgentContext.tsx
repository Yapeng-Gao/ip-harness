/**
 * Compat re-export — Agent context lives in @ip/app-state (Phase 2).
 */
export { AgentProvider, useAgents } from '@ip/app-state'
export type {
  HitlSessionAction,
  HitlActionResult,
  AgentContextValue,
  SessionSearchState,
} from '@ip/app-state'
